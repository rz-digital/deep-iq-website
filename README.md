# DeepIQ website

Page structure is stored in `public/page.json`. Editable page copy, navigation, links, metrics, cards, form messages, and footer details are stored in `public/site-content.json`.

Run `npm run dev` for local development and `npm run build` to regenerate `dist`.
